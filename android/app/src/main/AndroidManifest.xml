<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      Permission justifications — used when filling out the Play
      Console's Data Safety form. Keep this comment block in sync
      with what's actually requested below; the Play Store reviewer
      will reject the submission if the form claims a permission is
      unused but the manifest still requests it.

      INTERNET
        Required by every network call to the rebuzzpos backend
        (auth, catalogue, cart, ticket creation, business logo,
        notifications). No data collection beyond what the user
        actively submits.

      ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION
        Used ONLY by the "Use my current location" button on the
        New Address screen, to autofill the customer's street
        address. Lat/lng is sent to OpenStreetMap Nominatim for
        reverse-geocoding (returns a human-readable address); the
        coordinates are then attached to the saved address record
        on our backend so the bakery knows where to deliver.
        Not collected in the background, not used for tracking,
        not shared with third parties beyond Nominatim.

      No other permissions are requested.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="Pokhara Bakery"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
